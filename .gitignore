# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
.venv/
env/
myenv/

# Build artifacts
build/
dist/
*.egg

# IDE / editor
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Local env / secrets
.env
.env.local
*.local

# Model caches and weights (must NEVER be committed)
models/
weights/
*.pt
*.bin
*.safetensors
*.gguf
*.onnx

# Runtime / logs
logs/
*.log
runtime/

# Scratch / dev-only one-shot scripts
scratch/

# Claude Code session/agent local state — per-developer, not project state
.claude/

# Captured request/response payloads from the observability layer
# (default-off; when a dev opts in and AISTACK_OBS_PAYLOAD_DIR is unset,
# captures land here).
captures/
